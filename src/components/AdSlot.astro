---
const enabled = import.meta.env.PUBLIC_ADSENSE_ENABLED === 'true';
---
{enabled && (
  <aside class="ad-slot" aria-label="Publicidade">
    <!-- Inserir aqui o bloco oficial do AdSense após a aprovação e a configuração do publisher ID. -->
  </aside>
)}
